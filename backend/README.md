# SpamShield Backend

## Endpoints

- GET /api/health
- POST /api/analyze
- GET /api/jobs/:jobId
- GET /api/jobs/stream/:jobId
- GET /api/history

## Run

Use the root project scripts to start the server and frontend separately.
